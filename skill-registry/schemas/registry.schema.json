{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["schema_version", "skills"],
  "properties": {
    "schema_version": {"type": "string"},
    "skills": {"type": "array", "items": {"type": "object", "required": ["name", "version", "category", "path", "status", "manifest"]}}
  }
}
