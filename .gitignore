# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local credentials and configuration
config/api_keys.json

# Private Brain data (encrypted vault + audit log) — never commit
memory/private_brain/
